欲实现的功能
一、主界面功能
    1、配置文件读取写入和读取中文路径
    2、网口向硬件发送数据，
    3、网口接收数据，解析数据包，
    4、保存数据。

    1、开始测量（手动测量），文件存储命名以时分秒来备份保存，测量结束时弹出窗口提醒用户重命名。
    2、备份文件集成到自动测量按钮中，一打开便弹出弹窗，提醒用户设置存储路径。
    3、自动保存上一次的存储文件目录，保存到json文件中，并在下一次选择目录时直接打开上一次目录。
       判断是否存在默认路径，不存在则读取桌面所在目录。
    4、四个网口存储的界面，新开一个项目。多线程处理数据。

已具备的功能：
一、主界面
    1、程序生成的可执行文件图标换为nuclear.ico。
    2、JSON读取以及写入中文路径。